import { Button } from "@/components/button";

export function ConnectProfile() {
  return (
    <div class="flex items-center justify-center">
      <Button variant="secondary">Connect idOS profile</Button>
    </div>
  );
}
