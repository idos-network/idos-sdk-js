{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "clean": {},
    "format": {},
    "lint": {},
    "test": {
      "dependsOn": ["@idos-network/idos-sdk#build"]
    }
  }
}
