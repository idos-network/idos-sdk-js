{
  // extract-zip has no published patched release yet. Both advisories are only
  // reachable via @size-limit/time → puppeteer (dev/CI bundle-size tooling).
  "allowlist": ["GHSA-jmr9-qjv8-65gv", "GHSA-7pqw-9j4j-h8q3"],
}
